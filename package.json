{
  "name": "kokokara",
  "version": "1.0.0",
  "scripts": {
  "dev": "npm run dev --prefix frontEnd",
  "test": "echo \"Error: no test specified\" && exit 1"
},
  "dependencies": {
    "firebase": "^12.13.0"
  }
}
